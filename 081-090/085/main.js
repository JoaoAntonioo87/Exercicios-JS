// Crie um módulo chamado math.js contendo três funções: sum, subtract e multiply. Importe todas elas em outro arquivo e utilize cada uma.

import { sum, subtract, multiply } from "./math.js";

console.log(sum(120, 56));
console.log(subtract(120, 56));
console.log(multiply(10, 56));
