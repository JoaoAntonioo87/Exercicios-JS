// Crie um arquivo utils.js que contenha uma função responsável por transformar um texto em letras maiúsculas. Em seguida, importe essa função em main.js e exiba o resultado no console.
import maiusculas from "./utils.js";

const frase = "olá mundo!";

console.log(maiusculas(frase));
