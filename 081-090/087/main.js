// Crie um módulo que exporte uma função principal como padrão e uma constante chamada VERSION como exportação nomeada. Importe ambos em outro arquivo.

import principal, { VERSION } from "./function.js";

console.log(principal(), VERSION);
